language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-spritesmith' ]; then cd .. && eval "mv $currentfolder generator-spritesmith" && cd generator-spritesmith; fi

